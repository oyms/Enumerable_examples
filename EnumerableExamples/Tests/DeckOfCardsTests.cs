namespace EnumerableExamples.Tests;

public class DeckOfCardsTests
{
    [Test]
    public void GetAllCards()
    {
        var @out = TestContext.Current!.OutputWriter;
        //var target = new DeckOfCards();
        //foreach(var card in target)
        //{
        //  @out.WriteLine(card);
        //}
    }
}